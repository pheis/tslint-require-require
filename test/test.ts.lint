const foo = require('foo');
                     ~~~      [File not found]

const asdf = 'asdf'

const foo = require('./test.ts.lint');
const foo = require('../package.json');

const foo = require('../package.notFound');
                     ~~~~~~~~~~~~~~~~~~~      [File not found]
